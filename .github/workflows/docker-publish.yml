name: Build and Push Docker Image

on:
  push:
    branches: [main]
    tags:
      - 'v*'
  workflow_dispatch:

jobs:
  docker:
    runs-on: ubuntu-latest
    timeout-minutes: 20

    steps:
      - uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to container registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ghcr.io/${{ github.repository_owner }}/fusion-council-backend
          tags: |
            type=sha,prefix=sha-
            type=semver,pattern={{version}}

      - name: Build and push
        uses: docker/build-push-action@v5
        with:
          context: .
          push: ${{ github.event_name != 'workflow_dispatch' }}
          tags: ${{ steps.meta.outputs.tags }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
          load: ${{ github.event_name == 'workflow_dispatch' }}
          outputs: type=docker,dest=/tmp/fusion-council-api.tar

      - name: Run smoke test (when not workflow_dispatch)
        if: github.event_name != 'workflow_dispatch'
        run: |
          docker load --input /tmp/fusion-council-api.tar
          docker run --rm ghcr.io/${{ github.repository_owner }}/fusion-council-backend:sha-${{ github.sha }} --help || true

      - name: Checkout k8s-workbench
        if: github.event_name != 'workflow_dispatch' && github.ref == 'refs/heads/main' && ${{ secrets.K8S_WORKBENCH_PUSH_TOKEN != '' }}
        uses: actions/checkout@v5
        with:
          repository: karabayogo/k8s-workbench
          token: ${{ secrets.K8S_WORKBENCH_PUSH_TOKEN }}
          path: k8s-workbench

      - name: Update k8s-workbench dev image tag
        if: github.event_name != 'workflow_dispatch' && github.ref == 'refs/heads/main' && ${{ secrets.K8S_WORKBENCH_PUSH_TOKEN != '' }}
        run: |
          python <<'PY'
          from pathlib import Path

          path = Path('k8s-workbench/values/fusion-council-api/dev.yaml')
          original = path.read_text()
          lines = original.splitlines()
          updated = []
          in_image_block = False
          replaced = False

          for line in lines:
              stripped = line.strip()
              if stripped == 'image:':
                  in_image_block = True
                  updated.append(line)
                  continue
              if in_image_block and line and not line.startswith(' '):
                  in_image_block = False
              if in_image_block and stripped.startswith('tag:'):
                  updated.append('  tag: "sha-${{ github.sha }}"')
                  replaced = True
                  continue
              updated.append(line)

          if not replaced:
              raise SystemExit('image.tag not found in values/fusion-council-api/dev.yaml')

          path.write_text('\n'.join(updated) + '\n')
          PY

      - name: Commit k8s-workbench dev image tag
        if: github.event_name != 'workflow_dispatch' && github.ref == 'refs/heads/main' && ${{ secrets.K8S_WORKBENCH_PUSH_TOKEN != '' }}
        run: |
          cd k8s-workbench
          if git diff --quiet -- values/fusion-council-api/dev.yaml; then
            echo "No k8s-workbench changes to commit"
            exit 0
          fi
          git config user.email "ci@fusion-council.local"
          git config user.name "GitHub Actions CI"
          git add values/fusion-council-api/dev.yaml
          git commit -m "Update fusion-council-api dev image to sha-${{ github.sha }}"
          git push
